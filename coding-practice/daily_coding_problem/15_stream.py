"""
    Given a stream of elements too large to store in memory, pick a random element from the stream with uniform probability.

    Thoughts: You could set a random timer to select an element as it comes
    You could store the element s and then randomly choose from the subset with removal
"""

